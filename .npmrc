# @deepseek-ai/* are host-provided by the DSH environment; do not auto-install peers
# (their npm release chain is incomplete upstream: dsh-session -> dsh-type-meta is unpublished)
auto-install-peers=false
